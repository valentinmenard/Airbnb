<h1> Hello <% current_user.name %>   </h1>

<% @flats.each do |flat| %>
  <%= flat.title %>
  <%= flat.description %>
  <%= flat.picture_1 %>
<% end %>

<% link_to "Add a new flat", new_user_flat_path(@user) %>