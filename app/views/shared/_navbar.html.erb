<nav class="navbar navbar-default navbar-wagon" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header inline">
<!--       <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button> -->
      <a href="/">
        <ul class="list-inline navbar-brand">
          <li>
            <%= image_tag "new_tipi.svg" %>
          </li>
          <li>
            <p>SuprTipi</p>
          </li>
        </ul>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              <% if current_user.image(:thumb) %>
                <%= image_tag current_user.image(:thumb) , class: "img-avatar" %>
              <% else %>
                <%= image_tag "http://placehold.it/30x30" , class: "img-avatar" %>
              <% end %>
              <% if current_user.name? %>
                <%= current_user.name %> <span class="caret"></span>
              <% else %>
                Profile <span class="caret"></span>
              <% end %>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to profile_users_path do %>
                  <i class="fa fa-user"></i> Profile
                <% end %>
              </li>
              <li>
                <%= link_to account_flats_path do %>
                  <i class="fa fa-home"></i> My Flats
                <% end %>
              </li>
              <li>
                <%= link_to bookings_sent_users_path do %>
                  <i class="fa fa-paper-plane"></i> Bookings sent
                <% end %>
              </li>
              <li>
                <%= link_to bookings_received_account_bookings_path do %>
                  <i class="fa fa-stack-overflow"></i> Bookings received
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i> Sign out
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t(".sign_up", default: "Sign up"), new_user_registration_path %>
          </li>
          <li>
            <%= link_to t(".sign_in", default: "Log in"), new_user_session_path %>
          </li>
        <% end %>
        <li>
          <%= link_to t(".top_call_to_action", default: "Rent your flat"), new_account_flat_path, class: "btn" %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>